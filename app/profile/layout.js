export const metadata = {
    title: "Profile",
    description: "Your Profile Page",
};

export default function ProfileLayout({ children }) {
    return (
        <>
            {children}
        </>
    );
}
