export const metadata = {
    title: "Login",
    description: "Login to OraLink",
};

export default function LoginLayout({ children }) {
    return (
        <>
            {children}
        </>
    );
}
