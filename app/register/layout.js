export const metadata = {
    title: "Register",
    description: "Register to OraLink",
};

export default function RegisterLayout({ children }) {
    return (
        <>
            {children}
        </>
    );
}
